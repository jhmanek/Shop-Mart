import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import ReduxProvider from "@/app/cart/redux";
import ClientLayoutWrapper from "@/components/nav-clinetside";
import Head from "next/head";
import Providers from "./provider";
import { Toaster } from "react-hot-toast";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
  display: "swap",
});
<Toaster
  position="top-right"
  toastOptions={{
    style: {
      padding: 0,
      borderRadius: "8px",
      boxShadow: "none",
    },
  }}
/>;

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
  display: "swap",
});

export const metadata: Metadata = {
  title: "Create Next App",
  description: "Generated by create next app",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <Head>
        <title>MyStore - Home</title>

        <link rel="shortcut icon" href="/latest/public/msfavicon.svg" />
        <link
          rel="icon"
          href="/latest/public/msfavicon.svg"
          type="image/svg+xml"
        />
      </Head>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
        suppressHydrationWarning
      >
        <ReduxProvider>
          <Providers>
            <ClientLayoutWrapper>{children}</ClientLayoutWrapper>
          </Providers>
        </ReduxProvider>
        <Toaster position="top-right" reverseOrder={false} />
      </body>
    </html>
  );
}
